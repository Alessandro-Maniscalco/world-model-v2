I would like to do a controller using the codex session, so that it eventually loops and finds the right strucutre for the model.
Ideally you look at the code, potentially make changes, run a command, end the codex session, then resume the codex session once the command is done, validate the results, and repeat.

As an itial test, run sleep 10 s, end the chat and once finished running resume the codex command. you might run sleep && resume, or whatever else you need to do, I dont know how you can do it but I trust you